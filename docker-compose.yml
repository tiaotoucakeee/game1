services:
  game:
    build: .
    ports:
      - "3000:3000"
    environment:
      ANI_KIMI_API_KEY: ${ANI_KIMI_API_KEY:-}
      ANI_KIMI_MODEL: ${ANI_KIMI_MODEL:-moonshot-v1-8k}
    restart: unless-stopped
